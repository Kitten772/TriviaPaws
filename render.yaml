services:
  - type: web
    name: purrfect-trivia
    env: node
    buildCommand: npm install && ./build-render.sh
    startCommand: node render-start-fixed.js
    envVars:
      - key: NODE_ENV
        value: production